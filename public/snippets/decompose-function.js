const renderColumnSums = function() {
   //   iterate through each column on the spreadsheet 
   //     and pass each column through getsNumericalValue() 
   //     to get an array of numerical values
   //   pass the new through sumOfNumericalValuesInColumn() 
   //     to get the sum
   //   find the footer element for this column
   //   set its text content to the sum

};

const getsNumericalValue = function() {
  //   fetch all the cell values for that column from the model
  //   filter out the non-numeric values from this list
};

const sumOfNumericalValuesInColumn = function() {
  //   add up the filtered values in the list
};