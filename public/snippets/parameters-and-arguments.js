const greenify = function(el) {
  el.setAttribute('style', 'background-color: green;')
};

greenify();

//I don't know how to do this yet.  I looked up the documentation
//  setAttribute() but the documentation suggests that it takes
//  2 arguments which would make the code above correct.